import { conf } from './base-config';
{{#if isMocha}}
import assert from 'assert';
{{/if}}

{{#if isJasmine}}
describe('replace configuration test suite', () => {
  it('title is required', () => {
    expect(conf.title).toBe('Packer CLI');
  });

  it('config type should be replace', () => {
    expect(conf.configType).toBe('BASE');
  });
});
{{/if}}
{{#if isMocha}}
describe('replace configuration test suite', () => {
  it('title is required', () => {
    assert.equal(conf.title, 'Packer CLI');
  });

  it('config type should be replace', () => {
    assert.equal(conf.configType, 'BASE');
  });
});
{{/if}}
{{#if isJest}}
describe('replace configuration test suite', () => {
  test('title is required', () => {
    expect(conf.title).toBe('Packer CLI');
  });

  test('config type should be replace', () => {
    expect(conf.configType).toBe('BASE');
  });
});
{{/if}}
