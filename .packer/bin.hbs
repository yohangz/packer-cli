#!/usr/bin/env node

const { initPacker } = require('../bundle/{{packageName}}.{{format}}.min.js');
initPacker().then(() => {
  // no implementation
}, (e) => {
  console.error('packer failure: ', e);
}).catch((e) => {
  console.error('packer crash: ', e);
});
