#!/usr/bin/env node

const { initPacker } = require('../bundle/{{packageName}}.{{format}}.min.js');
initPacker().then(() => {
  // no implementation
}, (e) => {
  console.error(e);
}).catch((e) => {
  console.error(e);
});
